// Button.tsx — PAGE BLANCHE. Le deuxième geste complet : un composant avec comportement (disabled, loading, asChild).
// L'oracle (test/) importe `Button` et `ButtonProps` depuis `@lab/Button`. Contrat exact : README § Vérifier.
// Tokens fournis dans ./tokens.css ; classes dans ./Button.css (à créer).

export {};
