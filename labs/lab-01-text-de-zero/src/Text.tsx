// Text.tsx — PAGE BLANCHE. C'est ICI que tu écris le composant, avec ses six endroits.
// L'oracle (test/) importe `Text` et `TextProps` depuis `@lab/Text`. Contrat exact : README § Vérifier.
// Les tokens sont fournis dans ./tokens.css ; les classes vivent dans ./Text.css (à créer).

export {};
