// Text.stories.tsx — PAGE BLANCHE. Le catalogue des états de Text (CSF3 : `meta` par défaut + une story par export nommé).
// L'oracle rend chaque story exportée ici et exige au minimum : Default, Muted, Small. Contrat : README § Vérifier.

export {};
